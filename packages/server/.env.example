# Server Configuration
PORT=3002
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:5173

# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/scc_game?schema=public"

# Redis (for BullMQ job queue)
REDIS_URL="redis://localhost:6379"

# ===================================
# OpenAI Compatible API Configuration
# ===================================
# 支持 OpenAI 官方 API 和第三方兼容 API（如中转站、代理服务等）
#
# 方式1: 使用 OpenAI 官方 API
# OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_BASE_URL=                      # 留空使用默认 https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
#
# 方式2: 使用第三方 OpenAI 兼容 API（中转站）
# OPENAI_API_KEY=your-api-key
# OPENAI_BASE_URL=https://your-proxy.com/v1
# OPENAI_MODEL=gpt-4o-mini              # 根据中转站支持的模型填写
#
# 方式3: 使用 Google Gemini (via OpenAI 兼容层)
# OPENAI_API_KEY=your-gemini-api-key
# OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# OPENAI_MODEL=gemini-2.0-flash
#
# ===================================

# 注意：API Key 现在可以在游戏内设置！
# 启动游戏后，点击左侧导航栏的 ⚙️ 设置按钮配置 LLM API
# 配置会保存到 packages/server/data/llm-config.json

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
LLM_PROVIDER=openai

# JWT Secret (for future auth)
JWT_SECRET=your-jwt-secret-change-in-production